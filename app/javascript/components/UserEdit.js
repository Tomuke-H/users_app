import React from "react";

const UserEdit = (props) => {

    return (
        <div>
            <h2>Edit page</h2>
            <a href="/">Home</a>
            <a href="/users/">Back to all Users</a>
        </div>
    );
};

export default UserEdit;