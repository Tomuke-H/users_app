import React from "react";

const UserNew = (props) => {

    return (
        <div>
            <h2>New User page</h2>
            <a href="/">Home</a>
            <a href="/users/">Back to all Users</a>
        </div>
    );
};

export default UserNew;